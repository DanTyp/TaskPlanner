{% extends 'base.html.twig' %}

{% block body %}
    <div class="col-md-4 col-md-offset-4" style="margin-top: 65px; margin-bottom: auto">
        <h1>Category creation</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-large btn-success"/>
        <a href="{{ path('category_index') }}" class="btn btn-large btn-primary">Categories list</a>
        {{ form_end(form) }}


        
    </div>

{% endblock %}
