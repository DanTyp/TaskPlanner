{% extends 'base.html.twig' %}

{% block body %}
    <div class="col-md-4 col-md-offset-4" style="margin-top: 65px; margin-bottom: auto">
        <h1>Category</h1>

        <table class="table">
            <tbody>
{#                <tr>
                    <th>Id</th>
                    <td>{{ category.id }}</td>
                </tr>#}
                <tr>
                    <th>Name</th>
                    <td>{{ category.name }}</td>
                </tr>
            </tbody>
        </table>



        


        {#<a href="{{ path('category_edit', { 'id': category.id }) }}" class="btn btn-large btn-success">Edit</a>#}


        {{ form_start(delete_form) }}
        <a href="{{ path('category_index') }}" class="btn btn-large btn-success">Categories list</a>
        <input type="submit" value="Delete" class="btn btn-large btn-danger">
        {{ form_end(delete_form) }}
        

    </div>
{% endblock %}
