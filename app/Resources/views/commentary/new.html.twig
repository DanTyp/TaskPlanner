{% extends 'base.html.twig' %}

{# {% form_theme form 'bootstrap_3_layout.html.twig' %} #}

{% block body %}
    <body style="background-color: #ffc966">
        <div class="col-md-4 col-md-offset-4">
            <h1>Commentary creation</h1>

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Add commentary" />
            {{ form_end(form) }}

            <ul>
                <li>
                    <a href="{{ path('commentary_index') }}">Back to the list</a>
                </li>
            </ul>
        </div>
    </body>
{% endblock %}
